# Order Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: app-services
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      serviceAccountName: kubox-secrets-sa
      containers:
      - name: order-service
        image: 862016452072.dkr.ecr.us-east-2.amazonaws.com/kubox:order-service-latest
        ports:
        - containerPort: 8082
          name: http
        envFrom:
        - secretRef:
            name: kubox-common-secret
        env:
        - name: DB_NAME
          value: "order_db"
        volumeMounts:
        - name: secrets-store
          mountPath: /mnt/secrets
          readOnly: true
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          periodSeconds: 5
          failureThreshold: 40
          timeoutSeconds: 2
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: kubox-secrets

---
# Order Service
apiVersion: v1
kind: Service
metadata:
  name: order-svc
  namespace: app-services
spec:
  selector:
    app: order-service
  ports:
  - port: 8082
    targetPort: 8082
    name: http
  type: ClusterIP
